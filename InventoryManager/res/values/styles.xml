<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    <style name="FillFill">
    <item name="android:layout_width">fill_parent</item>
    <item name="android:layout_height">fill_parent</item>
    </style>
    <style name="FillWrap">
    <item name="android:layout_width">fill_parent</item>
    <item name="android:layout_height">wrap_content</item>
    </style>
    <style name="WrapFill">
    <item name="android:layout_width">wrap_content</item>
    <item name="android:layout_height">fill_parent</item>
    </style>
    
    <style name="Button" parent="@style/FillWrap">
    <item name="android:layout_width">fill_parent</item>
    <item name="android:layout_height">wrap_content</item>
    <item name="android:onClick">clicker</item>
    
    </style>
<style name="Editext_FWCenterGravity" parent="@style/FillWrap">
    <item name="android:layout_marginLeft">30dp</item>
    <item name="android:layout_marginRight">30dp</item>
    <item name="android:gravity">center</item>
    <item name="android:textSize">@dimen/Normal_Font</item>
</style>
<style name="LinkedTextView" parent="@style/FillWrap">
    <item name="android:textSize">@dimen/Normal_Font</item>
    <item name="android:textColor">@color/General_LinkText</item>
   <item name="android:onClick">clicker</item>
   <item name="android:clickable">true</item> 
</style>
<style name="TextViewContent" parent="@style/FillWrap">
   <item name="android:textSize">@dimen/Normal_Font</item>
   <item name="android:onClick">clicker</item>
   <item name="android:clickable">true</item> 
</style>
</resources>
